name: zuraffa_storage
description: S3/MinIO storage capability for Zuraffa — MinioClient and the artifact-upload hooks (optional companion package; issue #1661).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  minio: ^3.5.8
  zuraffa:
    path: ../..
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.0
